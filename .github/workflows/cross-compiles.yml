# Copyright 2021-2026 The OpenSSL Project Authors. All Rights Reserved.
#
# Licensed under the Apache License 2.0 (the "License").  You may not use
# this file except in compliance with the License.  You can obtain a copy
# in the file LICENSE in the source distribution or at
# https://www.openssl.org/source/license.html

name: Cross Compile

on:
  pull_request:
    paths-ignore:
      - 'doc/**'
      - '*.md'
      - '*.pod'
      - 'README*'
      - 'funding.json'
      - 'LICENSE.txt'
      - 'VERSION.dat'
  push:
    paths-ignore:
      - 'doc/**'
      - '*.md'
      - '*.pod'
      - 'README*'
      - 'funding.json'
      - 'LICENSE.txt'
      - 'VERSION.dat'

permissions:
  contents: read

jobs:
  cross-compilation:
    # Run the full test suite on push, and on pull requests labelled with
    # 'extended tests'.  Other pull requests only run the EVP tests.
    env:
      EXTENDED: ${{ github.event_name == 'push' || contains(github.event.pull_request.labels.*.name, 'extended tests') }}
    strategy:
      fail-fast: false
      matrix:
        # The platform matrix specifies:
        #   arch: the architecture to build for, this defines the tool-chain
        #         prefix {arch}- (unless prefix is overridden) and the Debian
        #         compiler package gcc-{arch} name (unless compiler is overridden).
        #   compiler: optional; explicit Debian package name for the compiler.
        #   prefix:   optional; cross-compiler binary prefix if different from {arch}-.
        #   libs: the Debian package for the necessary link/runtime libraries.
        #   target: the OpenSSL configuration target to use, this is passed
        #           directly to the config command line.
        #   fips:   set to "no" to disable building FIPS, leave unset to
        #           build the FIPS provider.
        #   tests: omit this to run all the tests using QEMU, set it to "none"
        #          to never run the tests, otherwise its value is passed to
        #          the "make test" command to allow selective disabling of
        #          tests.
        #   label: optional; custom label used for artifact naming.
        #   toolchain_url: optional; URL to download a pre-built toolchain archive.
        #   no_apt: optional; set to true to skip apt compiler installation.
        #   qemucpu: optional; string that describes CPU properties.
        #            The string will be used to set the QEMU_CPU variable.
        #   opensslcapsname: optional; string that describes the postfix of the
        #                    OpenSSL environment variable that defines CPU
        #                    capabilities. E.g. "foo" will result in an
        #                    environment variable with the name OPENSSL_foo.
        #   opensslcaps: optional; if opensslcapsname (see above) is set, then
        #                this string will be used as content for the OpenSSL
        #                capabilities variable.
        #   ppa:   Launchpad PPA repository to download packages from.
        platform: [
          {
            arch: i386-pc-msdosdjgpp,
            libs: libc-djgpp-dev libwatt-djgpp-dev djgpp-utils,
            target: no-threads 386 DJGPP,
            tests: none,
            ppa: jwt27/djgpp-toolchain
          }, {
            arch: aarch64-linux-gnu,
            libs: libc6-dev-arm64-cross,
            target: linux-aarch64,
            fips: no
          }, {
            arch: alpha-linux-gnu,
            libs: libc6.1-dev-alpha-cross,
            target: linux-alpha-gcc,
            fips: no
          }, {
            arch: arm-linux-gnueabi,
            libs: libc6-dev-armel-cross,
            target: linux-armv4,
            fips: no,
            tests: -test_includes -test_store -test_x509_store
          }, {
            arch: arm-linux-gnueabihf,
            libs: libc6-dev-armhf-cross,
            target: linux-armv4,
            fips: no,
            tests: -test_includes -test_store -test_x509_store
          }, {
            # ARM Cortex-M4 (bare-metal, Thumb-2)
            arch: arm-none-eabi,
            label: arm-cortex-m4,
            libs: libnewlib-arm-none-eabi libstdc++-arm-none-eabi-newlib,
            target: gcc -mcpu=cortex-m4 -mthumb no-shared no-threads no-dso no-async no-asm,
            fips: no,
            tests: none
          }, {
            # ARM Cortex-M0+ (bare-metal, Thumb-1)
            arch: arm-none-eabi,
            label: arm-cortex-m0plus,
            libs: libnewlib-arm-none-eabi,
            target: gcc -mcpu=cortex-m0plus -mthumb no-shared no-threads no-dso no-async no-asm,
            fips: no,
            tests: none
          }, {
            # ARM Cortex-R5 (bare-metal real-time core)
            arch: arm-none-eabi,
            label: arm-cortex-r5,
            libs: libnewlib-arm-none-eabi,
            target: gcc -mcpu=cortex-r5 -mthumb no-shared no-threads no-dso no-async no-asm,
            fips: no,
            tests: none
          }, {
            # gcc hppa seems to have some potential compiler issues
            # with -O2 on this platform, reduce optimization to -01
            arch: hppa-linux-gnu,
            libs: libc6-dev-hppa-cross,
            target: -static -O1 linux-generic32,
            fips: no,
            tests: -test_includes -test_store -test_x509_store
          }, {
            # RISC-V 32-bit Embedded (RV32IMAC bare-metal)
            arch: riscv64-unknown-elf,
            label: riscv32-embedded,
            libs: picolibc-riscv64-unknown-elf libnewlib-riscv64-unknown-elf,
            target: gcc -march=rv32imac -mabi=ilp32 no-shared no-threads no-dso no-async no-asm,
            fips: no,
            tests: none
          }, {
            # Xtensa (Tensilica) - ESP32 LX6
            arch: xtensa-esp32-elf,
            label: xtensa-esp32,
            toolchain_url: "https://github.com/espressif/crosstool-NG/releases/download/esp-14.2.0_20241119/xtensa-esp-elf-14.2.0_20241119-x86_64-linux-gnu.tar.xz",
            no_apt: true,
            prefix: xtensa-esp-elf-,
            target: gcc -mlongcalls no-shared no-threads no-dso no-async no-asm,
            fips: no,
            tests: none
          }, {
            # Microchip PIC32 (MIPS32r2)
            arch: mipsel-linux-gnu,
            label: pic32-mips,
            libs: libc6-dev-mipsel-cross,
            target: gcc -EL -mips32r2 -msoft-float no-shared no-threads no-dso no-async no-asm,
            fips: no,
            tests: none
          }, {
            arch: m68k-linux-gnu,
            libs: libc6-dev-m68k-cross,
            target: -static -m68040 linux-latomic -Wno-stringop-overflow,
            fips: no,
            tests: -test_includes -test_store -test_x509_store
          }, {
            arch: mips-linux-gnu,
            libs: libc6-dev-mips-cross,
            target: -static linux-mips32,
            fips: no,
            tests: -test_includes -test_store -test_x509_store
          }, {
            arch: mips64-linux-gnuabi64,
            libs: libc6-dev-mips64-cross,
            target: -static linux64-mips64,
            fips: no
          }, {
            arch: mipsel-linux-gnu,
            libs: libc6-dev-mipsel-cross,
            target: linux-mips32,
            fips: no,
            tests: -test_includes -test_store -test_x509_store
          }, {
            arch: powerpc64le-linux-gnu,
            libs: libc6-dev-ppc64el-cross,
            target: linux-ppc64le,
            fips: no
          }, {
            arch: riscv64-linux-gnu,
            # Pin the vector spec version, otherwise qemu-user emits a
            # "vector version is not specified" warning on stderr at every
            # process start, which upsets tests that parse or check stderr.
            qemucpu: "rv64,v=true,vext_spec=v1.0",
            libs: libc6-dev-riscv64-cross,
            target: linux64-riscv64,
            fips: no
          }, {
            arch: s390x-linux-gnu,
            libs: libc6-dev-s390x-cross,
            target: linux64-s390x,
            fips: no
          }, {
            arch: sh4-linux-gnu,
            libs: libc6-dev-sh4-cross,
            target: no-async linux-latomic,
            fips: no,
            tests: -test_includes -test_store -test_x509_store
          },

          # These build with shared libraries but they crash when run
          # They mirror static builds above in order to cover more of the
          # code base.
          {
            arch: hppa-linux-gnu,
            libs: libc6-dev-hppa-cross,
            target: linux-generic32,
            tests: none
          }, {
            arch: m68k-linux-gnu,
            libs: libc6-dev-m68k-cross,
            target: -mcfv4e -mxgot linux-latomic -Wno-stringop-overflow no-quic,
            tests: none
          }, {
            arch: mips-linux-gnu,
            libs: libc6-dev-mips-cross,
            target: linux-mips32,
            tests: none
          }, {
            arch: mips64-linux-gnuabi64,
            libs: libc6-dev-mips64-cross,
            target: linux64-mips64,
            tests: none
          },

          # This build doesn't execute either with or without shared libraries.
          {
            arch: sparc64-linux-gnu,
            libs: libc6-dev-sparc64-cross,
            target: linux64-sparcv9,
            tests: none
          }
        ]
    runs-on: ubuntu-latest
    steps:
    - name: install package repository
      if: matrix.platform.ppa != ''
      run: |
        sudo add-apt-repository ppa:${{ matrix.platform.ppa }}
    - name: install custom toolchain
      if: matrix.platform.toolchain_url != ''
      run: |
        sudo mkdir -p /opt/toolchain
        curl -sSL "${{ matrix.platform.toolchain_url }}" | sudo tar -xJ -C /opt/toolchain --strip-components=1 2>/dev/null || \
        curl -sSL "${{ matrix.platform.toolchain_url }}" | sudo tar -xz -C /opt/toolchain --strip-components=1
        echo "/opt/toolchain/bin" >> $GITHUB_PATH
    - name: install packages
      if: matrix.platform.no_apt != true
      run: |
        sudo apt-get update
        sudo apt-get -yq --allow-unauthenticated --allow-downgrades --allow-remove-essential --allow-change-held-packages install \
            ${{ matrix.platform.compiler || format('gcc-{0}', matrix.platform.arch) }} \
            ${{ matrix.platform.libs }}
    - uses: actions/checkout@v6
      with:
        persist-credentials: false
    - name: checkout fuzz/corpora submodule
      run: git submodule update --init --depth 1 fuzz/corpora

    - name: config with FIPS
      if: matrix.platform.fips != 'no'
      run: |
        ./config --banner=Configured --strict-warnings enable-fips enable-lms \
                 --cross-compile-prefix=${{ matrix.platform.prefix || format('{0}-', matrix.platform.arch) }} \
                 ${{ matrix.platform.target }}
    - name: config without FIPS
      if: matrix.platform.fips == 'no'
      run: |
        ./config --banner=Configured --strict-warnings enable-lms \
                 --cross-compile-prefix=${{ matrix.platform.prefix || format('{0}-', matrix.platform.arch) }} \
                 ${{ matrix.platform.target }}
    - name: config dump
      run: ./configdata.pm --dump

    - name: make
      run: make -s -j4

    - name: install qemu
      if: matrix.platform.tests != 'none'
      run: sudo apt-get -yq --allow-unauthenticated --allow-downgrades --allow-remove-essential --allow-change-held-packages install qemu-user

    - name: Set QEMU environment
      if: matrix.platform.qemucpu != ''
      run: echo "QEMU_CPU=${{ matrix.platform.qemucpu }}" >> $GITHUB_ENV

    - name: Set OpenSSL caps environment
      if: matrix.platform.opensslcapsname != ''
      run: echo "OPENSSL_${{ matrix.platform.opensslcapsname }}=\
                 ${{ matrix.platform.opensslcaps }}" >> $GITHUB_ENV

    - name: get cpu info
      if: matrix.platform.tests != 'none'
      run: |
         cat /proc/cpuinfo
         QEMU_LD_PREFIX=/usr/${{ matrix.platform.arch }} ./util/opensslwrap.sh version -c
    - name: make all tests
      if: env.EXTENDED == 'true' && matrix.platform.tests == ''
      run: |
        .github/workflows/make-test \
                  TESTS="-test_afalg" \
                  QEMU_LD_PREFIX=/usr/${{ matrix.platform.arch }}
    - name: make some tests
      if: env.EXTENDED == 'true' && matrix.platform.tests != 'none' && matrix.platform.tests != ''
      run: |
        .github/workflows/make-test \
                  TESTS="${{ matrix.platform.tests }} -test_afalg" \
                  QEMU_LD_PREFIX=/usr/${{ matrix.platform.arch }}
    - name: make evp tests
      if: env.EXTENDED != 'true' && matrix.platform.tests != 'none'
      run: |
        .github/workflows/make-test \
                  TESTS="test_evp*" \
                  QEMU_LD_PREFIX=/usr/${{ matrix.platform.arch }}
    - name: save artifacts
      if: success() || failure()
      uses: actions/upload-artifact@v5
      with:
        name: "cross-compiles@${{ matrix.platform.label || matrix.platform.arch }}"
        path: artifacts.tar.gz
        if-no-files-found: ignore
